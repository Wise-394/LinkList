import type { NextFunction, Request, response } from 'express';

export function updateUserInfo(
  req: Request,
  res: Response,
  next: NextFunction,
) {
  try {
  } catch (error) {
    next(error);
  }
}
